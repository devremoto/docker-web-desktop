# Docker Web Desktop Backend Configuration

# Server Configuration
PORT=3334

# Docker Configuration
# WSL2 Docker Daemon Connection
# Option 1: TCP connection (requires Docker daemon to expose TCP socket)
WSL2_DOCKER_HOST=localhost
WSL2_DOCKER_PORT=2375

# Option 2: Named pipe or socket path
# WSL2_DOCKER_SOCKET=//./pipe/docker_engine_wsl
# Or access WSL2 socket via UNC path (if enabled):
# WSL2_DOCKER_SOCKET=\\wsl$\Ubuntu\var\run\docker.sock

# CORS Configuration
CORS_ORIGIN=http://localhost:5173
